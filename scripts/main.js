let canvas = document.getElementById("gameWindow");

let ctx = canvas.getContext("2d");
let viewport_width = ctx.canvas.clientWidth;
let viewport_height = ctx.canvas.clientHeight;

// let img = document.getElementById("ext_atlas");
// ctx.drawImage(img, 0, 0, 64, 48, 100, 100, 128, 96);